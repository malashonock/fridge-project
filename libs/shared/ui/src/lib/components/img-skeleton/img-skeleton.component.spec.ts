import { ImgSkeletonComponent } from './img-skeleton.component';

describe('ImgSkeletonComponent', () => {
  it('should create an instance', () => {
    const directive = new ImgSkeletonComponent();
    expect(directive).toBeTruthy();
  });
});
